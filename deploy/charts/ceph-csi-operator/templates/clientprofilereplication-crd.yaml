apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: clientprofilereplications.csi.ceph.io
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  labels:
  {{- include "ceph-csi-operator.labels" . | nindent 4 }}
spec:
  group: csi.ceph.io
  names:
    kind: ClientProfileReplication
    listKind: ClientProfileReplicationList
    plural: clientprofilereplications
    singular: clientprofilereplication
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: ClientProfileReplication is the Schema for the clientprofilereplications
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ClientProfileReplication
            properties:
              localClientProfile:
                description: LocalClientProfile is the name of the local ClientProfile
                  CR
                type: string
              rbd:
                description: RBD contains RBD-specific replication configuration
                properties:
                  poolMapping:
                    description: PoolMapping maps local pool names to remote pool IDs
                    items:
                      description: PoolMappingSpec defines the mapping for a single
                        pool
                      properties:
                        name:
                          description: Name is the pool name (must be consistent across
                            clusters)
                          type: string
                        remoteID:
                          description: RemoteID is the pool ID on the remote cluster
                          type: string
                      required:
                      - name
                      - remoteID
                      type: object
                    type: array
                type: object
              remoteClientProfile:
                description: RemoteClientProfile is the name of the remote cluster's
                  client profile
                type: string
            required:
            - localClientProfile
            - remoteClientProfile
            type: object
          status:
            description: status defines the observed state of ClientProfileReplication
            properties:
              message:
                description: Message provides human-readable details about the current
                  phase
                type: string
              phase:
                description: Phase indicates the current state of this CR
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
