name: build
on:
  pull_request:
    branches:
      - main
      - release-*

# cancel the in-progress workflow when PR is refreshed.
concurrency:
  group: ${{ github.workflow }}-${{ github.event_name == 'pull_request' && github.head_ref || github.sha }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    name: build
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 1
      - uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version-file: go.mod
      - uses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2b # v5.3.0
        with:
          python-version: '3.x'
      - name: Install Python dependencies
        run: pip install pyyaml
      - name: run make build
        run: make build
      - name: make build installer
        run: make build-installer
      - name: make build openshift installer
        run: make build-openshift-installer
      - name: run build multifile installer
        run: make build-multifile-installer
      - name: Generate operator helm charts
        run: make build-helm-installer
      - name: Verify operator helm values are in sync
        run: make verify-helm-values
      - name: Generate operator helm document
        run: make generate-helm-docs
      - name: Check for uncommitted changes
        run: make check-all-committed
      - name: Show the uncommitted "git diff"
        if: ${{ failure() }}
        run: git diff ; false
